import styled from 'styled-components';

export const SelectStyles = styled.div`
  display: flex;
  flex-direction: column;
  margin-bottom: 1em;

  label{
    margin-bottom: 0.6em;
    font-weight: bold;
  }

  select{
    padding: 0.7em;
    border: none;
    border-radius: 0.5em;
  }
`;
