import Modal from "react-modal";


export function MenuModal({isOpen, onRequestClose}) {

  

  return (
    <Modal 
      isOpen={isOpen} 
      onRequestClose={onRequestClose}
    >
      
    </Modal>
  )
}